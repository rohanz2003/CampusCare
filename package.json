{
  "name": "campuscareschool",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "setup": "cd server && npm install && npm run seed && cd ../client && npm install",
    "dev": "concurrently \"npm run dev --prefix server\" \"npm run dev --prefix client\""
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}